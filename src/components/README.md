# components/

Reusable UI building blocks shared across pages.

- **ui/** — Small, generic pieces (buttons, cards, lists).
- **layout/** — Shell elements (navigation, footer, page wrapper).
- **common/** — Cross-cutting widgets (e.g. parallax) used in sections.

Keep co-located `ComponentName.css` next to a component when styles are component-specific.
