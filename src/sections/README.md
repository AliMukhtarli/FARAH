# sections/

Large, page-specific content blocks (hero, product grids, similar items). Grouped by page domain:

- **home/** — Landing page sections
- **catalog/** — Catalog hero and product rows
- **product/** — Product detail extras (similar, collections)

Sections are composed by `pages/`; they should not own routing.
